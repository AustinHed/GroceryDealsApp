{
  "name": "grocery-deals-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared",
    "firebase/functions"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @grocery-deals/web",
    "build": "npm run build --workspace @grocery-deals/web",
    "start": "npm run start --workspace @grocery-deals/web",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
